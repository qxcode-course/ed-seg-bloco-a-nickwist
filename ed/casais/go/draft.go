package main
import "fmt"
func main() {
    var quant, x, i int
    var animal []int

    fmt.Scan(&quant)

    for fmt.Scan(&x) == 1 {
        a = append(a, x)
    }

    for i=0; i<quant; i++
        if animal[i]

}
